# LAMBDA_REPOS is a comma-separated list of lambda repos that will be deployed by this terraform project
#    Will be picked up during tfplan and tfapply commands, cloned, and built using "npm run build"
LAMBDA_REPOS=https://github.com/leecookson/cookson_pro_api.git

LOAD_ZIPS=true